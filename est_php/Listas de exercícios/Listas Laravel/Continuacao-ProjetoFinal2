# Projeto de Continuação: API-FIRST - Evoluindo o SIGA-SAÚDE 🌐

**Objetivo:** Transformar a aplicação SIGA-SAÚDE em uma arquitetura profissional **API-First**. Isso significa que o núcleo do sistema será uma API RESTful segura e testável, preparando o terreno para ser consumida por qualquer tipo de cliente (um aplicativo mobile, um painel em outra tecnologia, ou um frontend JavaScript moderno).

-----

### ✅ Funcionalidades Essenciais (Agora como Endpoints de API)

O foco muda de "o que o usuário pode fazer na tela" para "quais recursos a API deve fornecer". As funcionalidades são as mesmas, mas agora são oferecidas como "serviços".

#### **Endpoints de Autenticação (`/api/auth`)**

  * `POST /login`: Recebe `email` e `senha`, retorna um **token de autenticação** se bem-sucedido.
  * `POST /logout`: Invalida o token do usuário logado.

#### **Endpoints de Pacientes (`/api/pacientes`)**

  * `GET /`: Lista todos os pacientes (com paginação).
  * `POST /`: Cria um novo paciente.
  * `GET /{paciente}`: Mostra os detalhes de um paciente específico.
  * `PUT/PATCH /{paciente}`: Atualiza os dados de um paciente.
  * `DELETE /{paciente}`: Exclui um paciente.

#### **Endpoints de Consultas (`/api/consultas`)**

  * Funcionalidades de CRUD semelhantes às de pacientes, retornando dados em JSON.

#### **Endpoints por Papel (Role)**

  * `GET /api/medico/consultas`: Endpoint especial que retorna apenas as consultas do médico autenticado.
  * `GET /api/admin/usuarios`: Endpoint para o administrador gerenciar os usuários do sistema.

-----

### 🛠️ Requisitos Técnicos e Guia de Implementação

#### **Setup e Autenticação via API (Aprofundamento Listas 19, 20)**

1.  **Refatoração:** Este projeto **continua** do anterior. Você não precisa criar um novo.
2.  **Autenticação:** A autenticação web do Breeze continua existindo, mas agora vamos focar na API. Configure o **Laravel Sanctum** (já instalado pelo Breeze) para autenticação baseada em tokens.
3.  **Lógica de Login:** O controller de login da API não irá mais redirecionar. Ele validará os dados e, se corretos, criará e retornará um token para o cliente: `return response()->json(['token' => $token]);`.

#### **Estrutura de Rotas e API Resources (Aprofundamento Listas 12, 20)**

  * **Ação:** Mova a lógica de `routes/web.php` para `routes/api.php`. As rotas em `web.php` se tornarão mínimas (talvez apenas para servir a página inicial da SPA).
  * **Ação:** Crie `API Resources` para `User`, `Paciente` e `Consulta`. Use-os em **todos** os controllers para formatar a saída JSON. Isso garante consistência e segurança, controlando quais dados são expostos.
      * `return new PacienteResource($paciente);` // Para um único item
      * `return PacienteResource::collection($pacientes);` // Para uma coleção

#### **Testes Automatizados (Novo Conceito Profissional)**

  * **Objetivo:** Garantir que cada endpoint da API funcione como esperado.
  * **Ferramenta:** Use o **Pest**, que já vem configurado no Laravel.
  * **Ação:** Crie um arquivo de teste: `php artisan make:test Api/PacienteTest`.
  * **Exemplo de Teste (`tests/Feature/Api/PacienteTest.php`):**
    ```php
    test('um usuário não autenticado não pode listar pacientes', function () {
        $this->getJson('/api/pacientes')->assertStatus(401); // Afirma que deu erro de "Não Autorizado"
    });

    test('um usuário autenticado pode listar pacientes', function () {
        $user = User::factory()->create(); // Cria um usuário de teste
        Sanctum::actingAs($user); // Simula que este usuário está logado via API

        $this->getJson('/api/pacientes')->assertStatus(200); // Afirma que a requisição foi um sucesso
    });
    ```

#### **Jobs e Filas (Novo Conceito Avançado)**

  * **Objetivo:** Executar tarefas lentas em segundo plano para que a resposta da API seja instantânea.
  * **Cenário:** Envio de e-mail de confirmação de consulta.
  * **Ação:**
    1.  Crie um Job: `php artisan make:job EnviarConfirmacaoConsultaJob`.
    2.  Coloque a lógica de envio de e-mail dentro do método `handle()` do Job.
    3.  Configure o driver de Fila para `database` no `.env` e rode `php artisan queue:table` e `php artisan migrate`.
    4.  No seu `ConsultaController`, no método `store`, após criar a consulta, despache o job: `EnviarConfirmacaoConsultaJob::dispatch($consulta);`.
    5.  O controller retorna a resposta JSON imediatamente, enquanto o job aguarda na fila para ser processado por um "worker" (`php artisan queue:work`).

#### **Frontend (Opcional, mas Recomendado)**

  * A aplicação web com Blade que você construiu se torna agora o primeiro "cliente" da sua API. Você pode:
    1.  **Refatorar com Inertia.js + Vue/React:** Substituir as views Blade por componentes JavaScript que fazem chamadas internas à sua API, criando uma SPA sem recarregamento de página. O Breeze pode te ajudar a iniciar essa estrutura.
    2.  **Manter como está:** Deixar a aplicação Blade funcional e tratar a API como um produto separado para ser consumido por terceiros (ex: um aplicativo mobile).

-----

### 🗺️ Passo a Passo Sugerido para a Evolução

1.  **Backup e Branching:** Antes de tudo, crie um novo branch no seu Git para esta nova versão: `git checkout -b feature/api-first`.
2.  **Configurar Sanctum:** Siga a documentação do Laravel para configurar a autenticação de API com Sanctum, criando as rotas de login/logout que retornam tokens.
3.  **Refatorar um CRUD:** Comece pelo CRUD de Pacientes. Mova as rotas para `api.php`, proteja-as com `auth:sanctum`, e modifique os métodos do `PacienteController` para retornar `PacienteResource` em vez de `view()`.
4.  **Escrever Testes para o CRUD:** Crie os testes para o CRUD de Pacientes. Garanta que todas as operações (listar, criar, ver, atualizar, deletar) estão funcionando e protegidas.
5.  **Implementar Jobs:** Escolha uma ação (como o agendamento de consulta), crie o Job para o envio de e-mail/notificação e refatore o controller para despachar o Job em vez de executar a tarefa diretamente.
6.  **Repetir:** Aplique o mesmo padrão (Refatorar para API -\> Escrever Testes -\> Implementar Jobs) para os outros módulos (Consultas, Usuários).
7.  **(Opcional) Conectar o Frontend:** Escolha uma abordagem de frontend (Inertia ou SPA separada) e comece a construir a interface que consome sua nova e robusta API.

Este projeto te levará do nível de um desenvolvedor que sabe usar um framework para o de um arquiteto de software que entende como projetar sistemas modernos e desacoplados.
